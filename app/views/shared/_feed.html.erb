<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %> <%# static_pages_controllerに定義 %>
  </ol>
  <%= will_paginate @feed_items, params: { controller: :static_pages, action: :home } %> 
<% end %>